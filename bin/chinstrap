#!/usr/bin/env python
import sys
import warnings
from chinstrap import main

if __name__ == "__main__":
    with warnings.catch_warnings():
        warnings.simplefilter("ignore", category=SyntaxWarning)
        exit(main(sys.argv))
